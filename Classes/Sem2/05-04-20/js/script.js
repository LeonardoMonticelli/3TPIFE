"use strict;";

const numbers = [1, 2, 3, 4, 4, 4, 5, 6, 6, 7, 78, 9, 1];

console.log(numbers.slice(5, 10));
//let newArray = numbers.slice(1, 3); // array.slice()

/*
Use slice on the numbers array to get a new array that will hold the folloing values:
4, 5, 6, 6, 7
 
Display the resulting array !
*/